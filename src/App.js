import { Search } from "@components";
import { WrapperApp } from "@layouts";
/**
 * * Main Wrapper
 *
 */
const App = () => {
    return (
        <WrapperApp>
            <Search />
        </WrapperApp>
    );
};

export default App;
